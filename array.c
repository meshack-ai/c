#include <stdio.h>
 int main()
 {
     int arrayA[5],i,n;
     printf("Enter the size of the array: ");
     scanf("%d", &n);
       printf("The array element are\n");
     scanf("%d", &arrayA[i]);
     for (i=0; i<n; i++)
     printf("%d\t", arrayA[i]);
     return 0;

 }